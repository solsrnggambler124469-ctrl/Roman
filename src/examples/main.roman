var name = "john rod";
var name2 = "[THE STRONGEST]";

print(name, name2, "%n);

var name2 = "john rod";

ifelse (ifcomp(name, name2, 0)) {
    print("hi", "%n");
} else {
    print("no", "%n");
};

func print2(str1, str2) {
    print(str1, str2, "%n");
};

var name2 = "johny rody";

table larp = ["albert", "einstein"];
table op = [name, name2, larp];

print2(op, " ");
print2(type(op), type(larp), " ");
print2(table_get_index(op, 2), " ");

var name2 = "rody johny";

print2(table_get_index(op, 2), " ");
print2(table_get_index(op, 3), " ");
print2(larp, " ");

var x = 1;

print("test", "%n");
