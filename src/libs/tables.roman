class String {
    "main" : ""
};

var times = 1;

func forfunc(){
    var times = times + 1;
};

func strcat(string1, string2) {
    return string1 + string2;
};

func strlen(string_table) {
    foreach(string_table, "forfunc");
    return times;
};

var base = "I'm the ";
var base = string_concat(base, "alpha");

table roman = ["r", "o", "m", "a", "n"];

print(strlen(roman));

print(base);
