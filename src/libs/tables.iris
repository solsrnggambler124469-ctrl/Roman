
func tappend(target_table, target_size, copied_table, copied_size) {
    var i = copied_size + 1;
    var max = target_size + copied_size;
    for (i: compare(i, max, 4): "++"){
        table_set_index(target_table, i, table_get_index(copied_table, i-copied_size));
    };
};
