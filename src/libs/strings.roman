class String {
    "main" : ""
};

var times = 0;

func forfunc(){
    times = times + 1;
};

func strcat(string1, string2) {
    return string1 + string2;
};

func strlen(string_table) {
    ForEach(string_table, "forfunc");
    return times;
};

var base = "I'm the ";
base = strcat(base, "alpha");

table roman = ["r", "o", "m", "a", "n"];

print(strlen(roman));

print(base);
